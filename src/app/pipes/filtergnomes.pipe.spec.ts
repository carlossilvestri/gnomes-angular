import { FiltergnomesPipe } from './filtergnomes.pipe';

describe('FiltergnomesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltergnomesPipe();
    expect(pipe).toBeTruthy();
  });
});
